---
globs: "**/*"
description: Ensure every new project initialized here includes minimal docs,
  license, and git attributes for consistent collaboration.
alwaysApply: false
---

Add a README, LICENSE (MIT by default), and .gitattributes (normalize EOLs, UTF-8) for all new repos