# Checkpoint RD-AMI — 2025-11-06 20:53

## Avances principales
- **Dashboard AMI listo** (`progressdashboard/`): parser (Node) reutiliza `scripts/lib/progress.js` para leer `PROYECTO.md`, calcular progreso real por fase/módulo y generar `data/project_data.json`. Frontend Tailwind (`index.html`, `app.js`, `styles.css`) muestra barras, badges de estado y lista de “Puntos de acción”.
- **Pipeline de despliegue** (`.github/workflows/deploy-status-site.yml` + `.deployment`): GitHub Actions genera el dataset y sube los archivos a Plesk vía FTP en `vcorp.mx/ami/progressdashboard`.
- **Correcciones aplicadas**: se eliminó el hardcode de llaves API en los demos (lector), se reescribió el commit para limpiar el historial y se documentó un nuevo checkpoint previo (`CHK_2025-11-06_1907.md`).
- **Backlog actualizado** continúa sincronizado con el JSON del dashboard (11 % global; Fase 0 en 28 %).

## Pendientes / Riesgos
- Verificar en el servidor Plesk que el workflow esté desplegando correctamente tras cada push (las credenciales FTP están en Secrets).
- US0.2 (entorno local/emuladores) sigue pendiente; siguiente prioridad en el backlog.
- Mantener un solo dashboard (status-site quedó como legado; considerarlo para limpieza futura).

## Próximas acciones sugeridas
1. Validar visualmente el dashboard en `https://vcorp.mx/ami/progressdashboard` después del próximo despliegue.
2. Avanzar con US0.2 (DevEx/emuladores) ahora que el tablero refleja los avances reales.
3. (Opcional) Eliminar el directorio `status-site/` si confirmamos que ya no se usa, para evitar confusiones.
