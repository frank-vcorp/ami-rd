# Checkpoint RD-AMI — 2025-11-06 21:59

## Avances
- `PROYECTO.md` actualizado para reflejar el status real: solo “Progreso AMI Dashboard” queda en `done` (100 %), el resto de módulos se marcó `pending` con progreso 0 y notas de tiempo (en espera de contrato/recurso).
- `progressdashboard/data/project_data.json` regenerado con los nuevos valores, listo para ser desplegado vía workflow/plesk y verificar que el tablero se mantiene sincronizado.

## Razón
Se requiere validar que el pipeline publique automáticamente los cambios (sin desplegar manualmente desde Plesk) dejando visibles los estados “pendiente” en el dashboard público.

## Próximos pasos
1. Empujar los commits (`chore(progress): update statuses pending contract` y este checkpoint) y esperar al workflow auto-commit para que suba la data.
2. Confirmar en `https://vcorp.mx/progress-ami/progressdashboard/` que solo la tarjeta de dashboard aparece completada.
